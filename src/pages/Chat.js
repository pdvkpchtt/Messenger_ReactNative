import { Text, View } from "react-native";

import Layout from "../hoc/Layout";

const Chat = () => {
  return (
    <Layout statusBar>
      <Text>chat</Text>
    </Layout>
  );
};

export default Chat;
